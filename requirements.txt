# Offline pipeline (Tasks 1-4 demo): `python run_all.py` works with these
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
tabulate>=0.9          # markdown result tables
mongomock>=4.1         # in-memory MongoDB for the offline DB / API / forecast demos

# Optional: use library models instead of the from-scratch NumPy ones
# scikit-learn>=1.3

# Live servers (used by docker-compose and for serving the real API)
# fastapi>=0.110
# uvicorn>=0.29
# pymysql>=1.1
# pymongo>=4.6
